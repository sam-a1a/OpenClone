# The whole app is first-party Kotlin with no reflection, so R8 is free to be
# as aggressive as it likes. The only entry points are the manifest components,
# which AGP keeps automatically.
-repackageclasses
-allowaccessmodification
